akka {
  loglevel = DEBUG
}

spray.can.server {
  request-timeout = 1s
  idle-timeout = infinite
}

db {
  dbUri = "jdbc:mysql://localhost:3306/rest"
  user = "rest"
  password = "changeme"
}

rest {
  host = "localhost"
  port = 8080

}

websocket {
  host = "localhost"
  port = 8082
}

logging {
  level = "DEBUG"
  package-level {
    com.codelouders.shmetterling.example.api.company = "ERROR"
    com.codelouders.shmetterling.example.api.person = "INFO"
  }
}